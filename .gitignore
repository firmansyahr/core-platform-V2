venv/
.env
__pycache__/
*.pyc
.DS_Store
*.parquet
frontend/node_modules/
frontend/.next/
# CATATAN (2026-06-20, dikoreksi setelah investigasi lebih lanjut): file
# loyalty_members.json di repo ini di-force-add ke git (git add -f) meski
# baris ignore di bawah ini tetap ada — TAPI ini HANYA dataset training
# Causal ML lokal (300 toko hasil regenerasi), TIDAK pernah dipakai production.
# Production punya Railway Volume sungguhan ("web-volume", mount /mnt/data,
# lihat api/routers/loyalty.py _get_data_dir()) yang menyimpan member ASLI
# hasil input pengguna — sepenuhnya terpisah dari file git ini. Lihat
# api/data/models/README.md untuk detail keterbatasan ini dan implikasinya
# ke endpoint /api/causal/store/{id_toko}.
api/data/loyalty_members.json
api/data/promos.json
api/data/cad_history.json
api/data/models/*
!api/data/models/causal_training_result.json
!api/data/models/gmm_training_result.json
!api/data/models/README.md
